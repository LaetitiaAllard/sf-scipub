{% extends 'base.html.twig' %}

{% block content %}
    <h1>Publications list</h1>
    <hr>
    <div class="table-responsive">
        <table class="table table-stripped table-condensed">
            <thead>
                <tr>
                    <th>Title</th>
                    <th>Author</th>
                    <th>Publishedat</th>
                    <th>Validated</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
            {% for publication in publications %}
                <tr>
                    <td>{{ publication.title }}</td>
                    <td>{{ publication.author }}</td>
                    <td>{% if publication.publishedAt %}{{ publication.publishedAt|date('Y-m-d H:i:s') }}{% endif %}</td>
                    <td>{% if publication.validated %}Yes{% else %}No{% endif %}</td>
                    <td>
                        <ul class="list-inline">
                            <li>
                                <a href="{{ path('app_admin_publication_show', { 'id': publication.id }) }}"
                                    class="btn btn-xs btn-primary">
                                    Show
                                </a>
                            </li>
                            <li>
                                <a href="{{ path('app_admin_publication_edit', { 'id': publication.id }) }}"
                                    class="btn btn-xs btn-warning">
                                    Edit
                                </a>
                            </li>
                        </ul>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
    <hr>
    <ul class="list-inline">
        <li>
            <a href="{{ path('app_admin_publication_new') }}" class="btn btn-sm btn-success">
                Create a new publication
            </a>
        </li>
    </ul>
{% endblock %}
